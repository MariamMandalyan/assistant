<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev: allow HTTP to local ERP (emulator 10.0.2.2, LAN IP) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
